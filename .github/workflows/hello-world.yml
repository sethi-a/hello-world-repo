name: Hello World Workflow

on:
  push:
    branches:
      - main

jobs:
    hello-job:
      name: hello-job
      runs-on: ubuntu-latest
      steps:
      - name: Echo Text
        run: echo "Hello, world!"
        